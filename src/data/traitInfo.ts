/**
 * Kept as the import path the UI already uses. The definitions themselves —
 * documentation and effects together, so they cannot drift apart — live in
 * `traits.ts`.
 */
export { traitInfo, TRAIT_DEFS } from './traits';
